## Deposit

Let's start taking deposits and moving them into the investment! 

Deposits from users will come in **DAI**. You can assume that before the user calls the `deposit` function they will have already approved this contract to spend their `amount` of DAI. 

Take the DAI from the user and invest it in uniswap v3 along the `path`.